name = "FactorVSA"
uuid = "fac70a5a-0000-4000-8000-000000000001"
authors = ["PRIMUS Team"]
version = "0.2.0"

[deps]
LinearAlgebra = "37e2e46d-f89d-539d-b4ee-838fcccc9c8e"
MORK = "162375cb-4023-4ef9-895b-22c0041d5c13"
Random = "9a3f8284-a2c9-5f02-9a11-845980a1fd5c"

[sources]
# Local-first dev-link to the sibling hardened (CognitiveSubstratesAI) MORK checkout.
# FactorVSA depends on MORK DIRECTLY (the phase-2 grounded-op shim); PathMap is NOT a
# direct dependency — it resolves transitively through MORK's own [sources]
# (Julia 1.11+ propagates a path-dep's [sources]; and [sources] only accepts direct
# deps). Flip to a git URL on org migration.
MORK = {path = "../MORK"}

[compat]
LinearAlgebra = "1"
MORK = "0.1"
Random = "1"
Test = "1"
julia = "1.12"

[extras]
Test = "8dfed614-e22c-5e08-85e1-65c5234f0b40"

[targets]
test = ["Test"]
